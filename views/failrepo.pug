extends layout

block content
  .container
    .row
      .col-12.grid-margin.stretch-card
        .card
          .card-body
            h4.card-title Fail
            p.card-description
              | Could not create a new repository. Maybe it already exists?
  script.